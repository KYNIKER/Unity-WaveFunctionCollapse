using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RuleSprite : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public List<RuleSprite> Self = new();

    public List<RuleSprite> Proximity = new();

    // Update is called once per frame
    void Update()
    {
        
    }
}
